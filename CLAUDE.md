# 项目通用工作规范

适用于 chaogecodex 名下所有仓库。放在仓库根目录，Claude Code 启动时自动读取；
使用 Codex 的仓库另存一份同名内容为 `AGENTS.md`。

本文件只放**跨项目通用**的规则。项目特有的约束写在末尾「本项目补充」一节，
具体的架构决策写在 `docs/adr/`。

---

## A. 判断与沟通

**A1** 保持诚实、基于证据的判断。发现问题和分歧要主动指出，不得为迎合而软化批评或夸大正面。
这条非可协商，优先级高于本文件其他任何一条。

**A2** 遇到障碍先自己找可行路径，再回报。不要用"做不到"当结论，也不要一上来列一堆限制条件劝退。
确实存在硬约束时，说清三件事：约束是什么、绕过去的代价、替代路径。

**A3** 操作类指引一次只说一步。但一步做完要主动给出下一步，不要等追问才动作。

---

## B. 任务交付规范

写给执行工具的任务书必须满足以下四条：

**B1 范围具体** — 指明确切的文件路径与模块名。禁止开放式的"自己找一下"。

**B2 路径绝对** — 所有路径写绝对路径，不写相对路径或"项目根目录下的……"。

**B3 前置状态写明** — 当前分支、依赖是否已安装、服务是否在运行、有哪些未提交改动。

**B4 数字必须是实测值** — 任何预期输出的数量、体积、耗时都必须来自实际测量，不得估算。
涉及近期改动过的数据，写任务书前先比对最新导出。

**B5** 以上四条的疏漏，责任在写任务书的一方，不在执行方，也不应由 Chaoge 承担。

---

## C. 完成标准

**C1** "任务完成"的报告必须附可验证证据：命令输出、日志、返回值、文件哈希、测试结果。
纯文字确认（"已完成""已修复"）不构成完成。

**C2** 无法提供证据时，明确说明哪一步没验证到，而不是省略不提。

---

## D. 外部依赖

**D1** 涉及云函数、三方 API、外部授权的，**先确认触发方式和鉴权路径，再写代码**。
不要写完代码再去试鉴权。

**D2** 任何带外部平台依赖的架构决策，当天完成验证，不推迟到后续实测阶段。

**D3** 沙箱 / CI 环境的网络出口限制属于外部依赖，同样适用 D1、D2。

---

## E. 协作链路

**E1** 最小化人工中继链。优先使用能直接操作代码库的工具，
避免"AI 输出 → 人工复制 → 另一个 AI 输入"这种链条。

**E2** 只有账号安全类操作（登录、密钥、授权、付款）保留给 Chaoge 本人执行。

---

## F. 决策记录

> 本节为 2026-09-05 新增，尚未经实战验证。试用一段时间后再决定是否保留。

**F1 判据：只记难以逆转的决策。** 可以随时改回来的选择不写进 ADR。
难以逆转 = 改动会波及多个模块、涉及数据迁移、影响已发布产物、或依赖外部平台配置。

**F2** 符合 F1 的决策写进 `docs/adr/NNNN-简短标题.md`，四段结构：
背景 / 决策 / 不可逆的原因 / 已知代价。

**F3 回读校验** — 提出新方案前，先检查是否与 `docs/adr/` 中已记录的决策冲突。
有冲突必须显式指出并说明为什么要推翻，不得默默覆盖。

**F4** 旧 ADR 被推翻时不删除，在文件顶部标注 `Superseded by NNNN`。

---

## G. 本文件的维护

> 本节为 2026-09-05 新增，尚未经实战验证。

**G1** 新增规则前，先检查是否已被现有条目覆盖。规则条数增长会稀释每条被遵守的概率。

**G2** 每条规则应能追溯到一次真实的踩坑。写不出触发它的具体事故，说明它还不该成为规则。

---

## 本项目补充

本仓库是「PPT 资料管家」：面向微信传播的 PPT 素材编排 H5。
前端 `index.html` 托管于 CloudBase，后端 `api/main.py` 跑 CloudBase Run + COS 持久卷。

- **邀请码明文不写入仓库或前端代码**，后端只保存 PBKDF2 哈希。这条是硬约束。
- CloudBase 环境 `ppt-ai-d8gx2g506c70eb54e`、CloudBase Run 后端、COS 持久卷都属外部平台
  依赖，适用 D1 / D2：改动触发方式或鉴权路径前先查清，不要写完代码再试鉴权。
- 本地验证（在 `api/` 目录下）：

  ```bash
  python -m pip install -r requirements.txt
  python -m unittest discover -s tests -p "test_*.py" -v
  ```

  线上端到端测试也在 `api/tests/`，跑它需要真实环境，属 C2 要说明的范围。
- 本仓库暂无 `docs/adr/`。出现符合 F1 的决策时新建该目录。
